# 2026-09-13 — NOVAForge Engine

## Engine evaluation (code audit + live test run)

Produced `Docs/Engine_Evaluation_2026-09-13.md`. Full evaluation of the codebase as built
(not the design doc). Headline: **the suite is NOT green** — the recorded "all green" state
in MEMORY.md no longer holds.

### Measured scale
- 36,665 hand-written LOC (Engine 18,787 · Editor 7,500 · Tests 8,961 · Samples 1,065 · Tools 352)
- 76,368 LOC third-party (Dear ImGui, stb)
- 202 tests / 1,245 `NF_CHECK` assertions across 7 suites

### Test results (build/DebugNinja, rebuilt 2026-09-13 18:25–18:33)
Core 36/36 ✅ · Jobs 4/4 ✅ · ECS 22/22 ✅ · Assets 13/13 ✅ · RHI 64/64 ✅ · Runtime 13/13 ✅
**EditorTests: 37/50 then SIGSEGV** — 12 tests never execute.

### Bugs found (all reproduced)
1. **CRITICAL — stale framebuffer cache → SIGSEGV.**
   `Renderer3D.hpp:217-229` keys `m_tonemap_fbs` on the raw `const rhi::Texture*` **address**.
   Cleared only in `destroy_resolution_dependent()` (Renderer3D.cpp:406,414), and
   `Renderer3D::resize()` early-returns on unchanged size (Renderer3D.cpp:424). So replacing the
   render target with a new texture of the *same size* → allocator recycles the address → cache
   hit → framebuffer built for the destroyed texture → dead `VkFramebuffer` in
   `vkCmdBeginRenderPass` → crash.
   Crashes: `material_albedo_gpu_effect`, `material_params_gpu_effect`,
   `hotreload_mesh_rebuilds_live`. All use a helper that creates a fresh 64×64 target per call
   while reusing one `Runtime`.
   Also: `Renderer3D.cpp:752` stores a possibly-null framebuffer and dereferences it unguarded
   at line 756.
   This violates the design doc's own "handle-based resources, no raw pointers" rule.
2. **HIGH — Triangle sample not validation-clean.** `Samples/Triangle/main.cpp:228-232` never
   sets `rp_desc.present_source = true` (default false, RHI.hpp:271) → one
   `vkQueuePresentKHR` layout error per frame. The "Validation: CLEAN" milestone claim does not
   hold for the sample. Sample also has no pixel verification.
3. **MEDIUM — false test failure.** `test_viewport.cpp:140` asserts `res.target.get() != first`
   after `res.reset()` destroyed `first`; the allocator recycles the address. Encodes an
   allocator detail as a contract.
4. **LOW** — GpuFixture reports `validation: off` while validation messages are emitted.
5. **LOW** — Benchmarks can't catch regressions: `benchmark_1m_entities` reports `[OK]` without
   running unless `NF_BENCH_1M=1` (green by skip), and all benchmark output goes through
   `NF_LOG_INFO` which `TestFramework.cpp` suppresses (min level = Warn). No thresholds.
6. **LOW** — No CI config, and **the project is not a git repository**.

### Strengths confirmed
- `Engine/RHI/include/NF/RHI/RHI.hpp` is genuinely backend-neutral: only std + `NF/Core/Types.hpp`
  includes; every "Vulkan" mention is a comment. Dynamic dispatch via `VulkanLoader`.
- `init(nullptr)` headless device enables 64 GPU tests without a window.
- `PipelineDesc` holds `const RenderPass*` (not a copy) — pipelines built against the exact pass.
- Per-swapchain-image render-finished semaphores.
- `RenderGraph` with automatic barriers + `ResourceState`.
- Only **2 TODO comments in 36,665 LOC** (both in JobSystem: thread affinity, worker tracking).

### Module status
Implemented: Core, Jobs, Platform, RHI, ECS, Scene, Rendering, Assets, Runtime, Editor.
**Empty placeholder dirs (0 files):** AI, Animation, Audio, Input, Networking, Physics,
Scripting, UI. (Input is actually implemented under `Platform/`.)

Roadmap: v0.1 essentially complete. v0.2 partial (Prefabs + Save only). v0.3–v1.0 not started.
Note: "World streaming from day one" is deferred, not implemented.

### Toolchain notes (re-confirmed)
- cmake/ninja/ctest are NOT on PATH; they live at
  `C:/Program Files (x86)/Microsoft Visual Studio/2022/BuildTools/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/`
  and `.../CMake/Ninja/ninja.exe`. `Scripts/build.sh` sets this up.
- `build_nf.bat` builds `build/DebugNinja` via VS 18 Community vcvars64.
- `build/DebugNinja` is the freshest and most complete build (includes Editor, EditorTests,
  PlatformTests, NFAssetCooker). `build/Debug` is stale (15:32) and lacks EditorTests.

### Fixes NOT applied
Deliberately left for Abdal — the framebuffer-cache fix is a design decision (resource id /
generation vs. lifetime token vs. explicit invalidation), not a mechanical patch. Precise
file:line and recommended direction are in the report.
