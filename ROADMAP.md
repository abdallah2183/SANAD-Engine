# خارطة طريق محرك سَنَد | SANAD Development Roadmap

توضح هذه الوثيقة مراحل بناء وتطوير **محرك سَنَد (SANAD Engine)** بقيادة وتأسيس **عبدالله**، حيث تبين ما تم إنجازه واختباره والمراحل الجارية والمستقبلية المفتوحة للمساهمة من المطورين.

---

## جدول المراحل البرمجية

| المرحلة (Phase) | الوصف | الحالة | المجالات المفتوحة للمساهمة |
| :--- | :--- | :---: | :--- |
| **Phase 1–3** | النواة الأساسية، RHI Vulkan، نظام الوظائف Jobs، معمارية ECS | **مكتمل** | صيانة وتحسين الأداء |
| **Phase 4–6** | محرر ImGui، التفتيش التفاعلي، مستعرض الأصول، التراجع والإعادة | **مكتمل** | تحسينات تجربة المستخدم |
| **Phase 7–8** | خط أنابيب الإضاءة المؤجلة PBR، RenderGraph، مواد وخامات | **مكتمل** | مصفوفات المواد |
| **Phase 9–10** | محرك الفيزياء الحتمي، نظام التحريك الهيكلي، صوتيات 3D | **مكتمل** | إضافة أشكال تصادم إضافية |
| **Phase 11** | استقلالية الطبقات، نظام الحفظ المتقدم، معمارية بث العوالم | **مكتمل** | اختبارات الاستقرار |
| **Phase 12** | مصفوفات تفاصيل النماذج (Dynamic Mesh LOD & Mesh Streaming) | **مكتمل** | خوارزميات التبسيط |
| **Phase 13** | خرائط الظلال الاتجاهية (Directional Shadows) وسماء إجرائية | **مكتمل** | تظليل الشيدرز، PCF |
| **Phase 14** | دمج MiniAudio للصوتيات واستيراد ملفات glTF 2.0 | **مكتمل** | استيراد الأصول، صيغ الصوت |
| **Phase 15** | ربط لغات البرمجة (C# / Lua) ودعم الخط العربي RTL في المحرر | **مكتمل** | بيئات التشغيل، خطوط HarfBuzz |
| **Phase 16** | محرك الفيزياء المتقدم Jolt Physics وشبكات اللعب المتعدد | **مكتمل** | عرض الوسائط |
| **Phase 17** | نسخ الأجسام والمفاصل على الشبكة (اتجاه المركبات، سجل الأجسام، مطبق القيود) | **مكتمل** | عرض الوسائط |
| **Phase 18** | طبقة ثنائية الأبعاد مستقلة (`NFScene2D`: أطلس، دفعة، خريطة بلاط، فيزياء 2D حتمية) | **مكتمل** | ظلال نقاطية، Bloom/SSAO |
| **Phase 19** | التدمير (`NFDestruction`: هندسة الكسر، أصول الشظايا، عالم الضرر، ميزانيات الحطام، `IDebrisSink` عبر Jolt) | **مكتمل** | تدمير الشبكات الحية |
| **Phase 20** | الظلال المتتالية المربوطة بالكاميرا (Cascaded Shadow Maps: تقسيم Lauritzen، أطلس 2×2، تثبيت التكسل، انحياز مشتق، مزج تسليم) | **مكتمل** | ظلال نقاطية |

---

## تفاصيل المراحل القادمة

### المرحلة 13: الظلال والسماء الإجرائية (Directional Shadows & Sky)
- [x] إضافة هدف ظلال بدقة 2048×2048 بصيغة `D32_SFloat`.
- [x] ممر ظلال اتجاهي عبر RenderGraph مع إسقاط متعامد (Orthographic Light Projection).
- [x] مرشح تنعيم الظلال 3×3 PCF مع موازنة الظلال (Shadow Bias).
- [x] سماء إجرائية متدرجة مع قرص الشمس دون الحاجة لخامات جاهزة (SkyParams + قرص/توهج قابل للضبط).
- [x] عناصر تحكم في المحرر لتفعيل الظلال وضبط شدتها (قوة الظل، الانحياز، قسم Sky كامل).
- [x] إعدادات السماء والظلال محفوظة في صيغة المشهد (`.nfscene`: سطر `Sky:` ومفاتيح `shadow_strength/bias`).

### المرحلة 14: الصوتيات واستيراد النماذج (Audio & glTF)
- [x] خط استيراد صوتي حقيقي: فك ترميز `WAV` و`OGG` و`MP3` و`FLAC` إلى `AudioBuffer` (miniaudio + stb_vorbis).
- [x] تحويل القنوات/معدل العينات + كشف الصيغة من البصمة السحرية.
- [x] بناء أداة استيراد لنماذج `glTF 2.0` (`.gltf`/`.glb`) وتحويلها إلى صيغة المحرك `.nfmesh` (أداة `NFModelImporter`).

### المرحلة 12: تفاصيل النماذج (LOD)
- [x] مولد LOD حتمي (تجميع شبكي لكل SubMesh) + بناء مستويات تلقائي ببحث ثنائي.
- [x] ربط الاختيار المسافي الموجود (`select_lod`) بمستويات مولدة ومختبرة.

### المرحلة 15: لغات البرمجة والتعريب (Scripting & Arabic UI)
- [x] دعم لغة **Lua 5.4** لكتابة منطق الألعاب (VM معزولة، مكتبة `nf.*`، ربط الكيانات، نظام `ScriptComponent`).
- [x] دعم تشكيل النصوص العربية (أشكال سياقية + ligatures + ترتيب RTL) داخل واجهة المحرر Dear ImGui (خط Amiri مدمج).
- [x] ترجمة واجهة المحرر (القوائم، النوافذ، الأقسام) إلى اللغة العربية مع زر تبديل فوري.

### المرحلة 16: فيزياء متطورة وشبكات ألعاب (Advanced Physics & Multiplayer)
- [x] طبقة نقل UDP غير حاجبة + قناة موثوقة (تسلسل/إقرار/إعادة إرسال) + لقطات حالة مُصدَرة.
- [x] سيرفر Authoritative بمحاكاة حركية مشتركة + تنبؤ العميل + مصالحة (مختبرة على loopback حقيقي).
- [x] دمج محرك الفيزياء العالمي **Jolt v5.6** (مكتبة static حتمية) + غلاف JoltWorld (كرة/صندوق/مستوى، سقوط/تكديس/حتمية مختبرة).
- [x] مركبات Jolt ونسخ القيود المعقدة (Jolt جاهز لها: Vehicle/Ragdoll/Constraints مبنية).
- [x] استعلامات المشهد على Jolt: Raycast + Sphere Sweep + استعلامات التراكب (Overlap Sphere/Box).
- [x] مستشعرات/مرسيات (Triggers/Sensors) لا تحجز الحركة + كشف التراكب.
- [x] تصادم مستمر (CCD / Linear Cast) للأجسام السريعة.
- [x] متحكم الشخصية على Jolt (§39): ارتفاع الدرجة (Step Offset) والمنحدرات، منصات متحركة، انحناء (Crouch)، وخطاف تسلق (Climb Hook)، مع حتمية التنبؤ الشبكي (CharacterVirtual).
- [x] أشكال تصادم معقّدة على Jolt: كبسولة (Capsule) + محدب (Convex Hull) + شبكة ساكنة (Static Mesh).
- [x] **نسخ القيود** (`clone_constraint`): استنساخ أي قيد بين جسمين جديدين مع حفظ النوع والحدود والمحاور — لبناء سلاسل/أراجيح/هياكل متكررة من قالب واحد.
- [x] **حالات العجلات وإعادة التعيين**: قراءة حالة كل عجلة (التماس، انضغاط التعليق، الدوران، الزاوية) لعرض النماذج، و`vehicle_reset` لنقل المركبة وتصفير سرعتها دون إعادة بنائها.
- [x] **نسخ المركبات والقيود على الشبكة**: مدخلات قيادة مشفرة (`VehicleDriveInput` بـ20 بايت عبر القناة الموثوقة)، لقطات هيكل دورية (`VehicleSnapshot` بسحر `NFVS` عبر UDP خام)، وأحداث مفاصل مرتبة (`ConstraintEvent` بسحر `NFCE`: إنشاء/حذف/نسخ لكل أنواع القيود السبع) — مع طابور مدخلات للسيرفر وأشباح Authoritative للعميل (مختبرة على loopback حقيقي).
- [x] **ربط C# عبر استضافة .NET 10** (`CSharpHost` بـnethost/hostfxr + صندوق `NFGameScript` المُدار): دورة Start/Update/Counter لكل كيان، استدعاءات راجعة للمضيف، وأنواع blittable فقط عبر الحدود — تهيئة واحدة لكل عملية (التفريغ يسمم إعادة التهيئة)، واختبارات الاستضافة تتخطى نفسها (NF_SKIP) عند غياب الرانتايم.
- [x] قماش/أجسام ناعمة (Cloth/Soft Body): شبكة جزيئات PBD بقيود هيكلية/قص/انحناء + تصادم مع كرة/صندوق/مستوى (محلّل داخلي).

### المرحلة 17: نسخ الأجسام والمفاصل على الشبكة (Body & Joint Replication)- [x] اتجاه المركبات في اللقطات (`NFVS` v2 بسجلات 44 بايت: quaternion وحدة مرفوض غيرها + `last_angle_correction` للأشباح) — الشبح الذي يتحرك بلا اتجاه يرسم سيارة تنزلق جانباً.
- [x] الاتجاه في `JoltBodyState` (ذري مع الموضع) وكتابته في `Transform` عبر `VehicleSystem` + `reset` يعيد الاستواء (صفر euler).
- [x] أحداث الأجسام (`BodyEvent` بسحر `NFBE`: إنشاء/حذف بتحقق صارم) + سجل `BodyRegistry` بأولوية الهرمية (static scenery لا يُقتلع).
- [x] المطبق `ConstraintApplier`: ترتيب (أجسام قبل مفاصل مع تأجيل)، idempotency، teardown المفاصل اليتيمة عند حذف/استبدال جسم، و`clone_constraint over the wire` end-to-end (مختبر بعوالم وهمية + قناة موثوقة حقيقية).
- [x] **إصلاح انقلاب الصورة (Vulkan Y-flip)**: الإسقاط كان OpenGL-style بدون قلب Y — كل إطار يُرسم مقلوباً عمودياً (يثبت بالرياضيات واللقطات: السيارة فوق عجلاتها). الإصلاح: `m[1][1]` سالبة في perspective/orthographic + `FrontFace::CW` لخطوط العمق/GBuffer/الالتقاط، مع اختبار `test_mat4_perspective_points_up_at_negative_ndc_y` الذي يمنع الانتكاس.
- [x] **ديمو قيادة مركبة** (`NFSampleVehicleDemo`): سيارة Jolt حقيقية بعجلات مرئية (موضع/توجيه/دوران من `wheel_states`) وكاميرا تتبع وبوابات ذهبية وصناديق قابلة للتحطيم — WASD للقيادة وSpace للفرامل وR لإعادة التعيين، مع لقطات موقع حقيقية.

### المرحلة 19: التدمير (Destruction — damage, fracture propagation, debris)
- [x] **فصل الطبقات عبر `IDebrisSink`**: `NFDestruction` حسابي بحت لا يلمس Jolt ولا GPU (الميزانية، تراكم الإجهاد، كينماتيكا الشظايا تُقرّر كلها بلا خلفية فيزيائية) ⇒ يُختبر على أي جهاز. `JoltDebrisSink` في `NFPhysics` هو التنفيذ الذي يحوّل `DebrisSpawn` إلى جسم Jolt محدّب — التبعية تسير باتجاه واحد فقط.
- [x] **عالم الضرر (`DestructionWorld`)**: تساقط خطي للمقدار، إجهاد متراكم على الروابط، وفصل الشجرة الفرعية عن الجزء المنفصل.
- [x] **تقسيم الحجم بدقة**: القطع المنبعثة تقسم المنطقة المنفصلة تماماً — لا تداخل ولا فجوة ولا انبعاث مزدوج (القطعة الواحدة تساوي اتحاد أحجام أبنائها ما دام رابطها الداخلي سالماً).
- [x] **الميزانية داخل `apply_damage` لا بعدها**: حد أقصى للروابط المكسورة لكل إطار (الضرر يؤجّل ولا يضيع)، سقف صارم للشظايا الحية يُقاعد الأقدم أولاً، وعمر أقصى. قنبلة في مستودع صناديق تُكبَّح بالتصميم لا بالضبط اللاحق.
- [x] **عدم حتمية كعقد**: لا RNG، لا ساعة، لا حاوية يعتمد ترتيبها على قيم المؤشرات؛ الروابط بترتيب الأصل، الشظايا بترتيب الشجرة، التقاعد بالعمر ثم بالوصول.
- [x] **`JoltDebrisSink`**: يعقّب مجموعه الحي الخاص لا `body_count()` العام — الميزانية سقف على *الحطام*، والعالم يحوي الأرضية والمركبات واللاعب أيضاً.
- [x] **`JoltWorld::set_angular_velocity`**: دوران الشظية يأتي من هبوط الدفعة بعيداً عن المركز؛ إسقاطه يجعلها تسقط مسطّحة.
- [x] جناحا `DestructionTests` 48/48 و `PhysicsTests` 189/189.

### المرحلة 20: الظلال المتتالية المربوطة بالكاميرا (Cascaded Shadow Maps)
- [x] **إصلاح الثغرة الجوهرية**: مصفوفة الظل كانت صندوقاً متعامداً `orthographic(-12,12,-12,12,1,60)` **مثبّتاً حول نقطة الأصل العالمية** والضوء عند `dir * -20`. النتيجة: أي كاميرا تبعد أكثر من ~12 وحدة عن الأصل تفقد ظلالها، وأي عالم أكبر من 24×24 وحدة لا ظل فيه إطلاقاً. الآن كل شريحة تُركَّب على زوايا frustum الكاميرا الحقيقية ⇒ الظلال تعمل في أي موضع من العالم.
- [x] **أطلس 2×2 بدل مصفوفة طبقات**: `VulkanFramebuffer` يأخذ `Texture*` ويستعمل عرضاً أحادي الطبقة، فدعم `View2DArray` كان سيتطلب تغيير RHI. الأطلس 2048×2048 (`1024` لكل شريحة) = **نفس ذاكرة اليوم بالضبط**، لكن بأربع مدىات بدل واحدة، مع `set_viewport/set_scissor` لكل شريحة داخل ممر RenderGraph واحد ⇒ حاجز واحد.
- [x] **تقسيم عملي (Lauritzen)**: `d_i = λ·n·(f/n)^(i/N) + (1-λ)·(n+(f-n)·i/N)` — الدقة حيث يقرأها اللاعب. الشريحة قابلة للضبط 1..4، مع `shadow_distance` لتضييق المدى (0 = مستوى الكاميرا البعيد).
- [x] **فرض كرويّة الفروق (Texel Snapping)**: الصندوق يُكمَّم لمضاعفات كاملة من حجم التكسل، وإلا اهتزّت حدود الظل مع كل حركة كاميرا. اختبار يقيس صندوقاً يغطي الشريحة عند كل إزاحة كسرية من التكسل — البداية لأسفل والنهاية لأعلى، وإلا قُصّ شريط بعرض تكسل عند المواضع غير المحظوظة.
- [x] **انحياز مُشتقّ لكل شريحة**: الانحياز الثابت كان خطأً فيزيائياً بعد التتالي — الانحياز بوحدات NDC يشتري إزاحة عالمية = `bias × مدى العمق`، والتتالي قلّص الاثنين بنسب مختلفة، فظهرت حبوب الظل (Acne) على الأوجه المضيئة. الآن `cascade_auto_bias` = `(حجم التكسل × tan θ) ÷ مدى العمق` لكل شريحة، **يُضاف** فوق `shadow_bias` الذي يضبطه الفنان ⇒ معناه ومداه `[0, 0.01]` لم يتغيّرا.
- [x] **مزج تسليم الشريحة (Cross-fade)**: تسليم صلب بين شريحتين بكثافتين مختلفتين يظهر كخط تتغيّر عنده حدة حدّ الظل؛ المزج داخل `fade_range` يخفيه، مع قصّ كل نقرة PCF داخل حدود شريحتها لمنع تسرّب الظل بين الشرائح.
- [x] **تحقق بلا GPU**: `ShadowCascades.hpp/.cpp` رياضيات خالصة (تقسيم، تركيب، تكسيل، اختيار شريحة، انحياز) بلا جهاز ⇒ تُختبر على أي آلة.
- [x] **اختبارات بكسل تكشف الانحدار بدقة**: صيغة تامة تساوي (`with_shadows == without` حرفياً) على وجه مضاء غير محجوب، وصفر تظليل ذاتي على أرضية بلا رامية — التصنيف الوحيد غير الملتبس بين حبوب الأرضية وظل المكعب. واختبار طاقة يثبت أن التتالي **يعيد توزيع** الظل دون أن يفقد منه شيئاً.
- [x] إعدادات متتالية في المشهد (`.nfscene`: `shadow_cascades`, `shadow_distance`) + شرائح في المفتش بتحقق وتراجع + مفاتيح تعريب `shadow_cascades`/`shadow_distance`.

### المرحلة 18: طبقة ثنائية الأبعاد المستقلة (`NFScene2D`)
- [x] وحدة CMake مستقلة عن Jolt/RHI: `Engine/Scene2D` (`DEPENDS NFCore`) مربوطة من الجذر.
- [x] أطلس رفّي حتمي (فرز الارتفاع ثم العرض ثم الاسم) + دفعة رباعيات بـ `stable_sort` على `(page, depth)`.
- [x] خريطة بلاط مقطّعة 16×16 بإحداثيات موقّعة، دمج تصادم جشع، A* ثماني مع كسر التعادل، وبث حلقات.
- [x] محلّل 2D: دائرة/صندوق، تجزئة مكانية، SAT، نبضات متتابعة، قيود مسافة/مفصل، مقابض جيل، `is_deterministic()`.
- [x] إضاءة 2D على الـ CPU بلا أي تبعية RHI: أضواء شعاعية/مخروطية، ثلاثة أنواع تلاشٍ، رمشة (`flicker`) هاش-مبذرة، وظلال مخبوزة على شكل إسفين (ظل الحافة المستقيمة = زاوية بين شعاعين تُقطع عند نصف القطر). الخبز والاختبار الشعاعي يتطابقان بِت-بِت أينما يصل الضوء، والإضاءة تُطوى في لون RGBA8 المكدوس بدل تمريرها للـ GPU.
- [x] أنظمة ECS: تحويل، تصيير مُضاء وغير مُضاء، إضاءة (تجمع الأضواء والحواجز من خريطة البلاط وتخبز)، خريطة بلاط، فيزياء، جسيمات — كلها على `nf::ecs::World` جنبًا إلى جنب مع لعبة 3D.
- [x] جناح `Scene2DTests`: 37/37 (أطلس، دفعة، بلاط، تنقل، محلّل 2D حتمي، مقابض، قيود، شعاع، محاذاة بكسل، إضاءة وظلال).

### إضافات خارج الخارطة (مكتملة)
- [x] دورة الليل والنهار (Time-of-Day: شمس وسماء ديناميكية).
- [x] توليد الأراضي الإجرائية (Heightfield + value-noise حتمي).
- [x] تسجيل وإعادة تشغيل المدخلات (Replay حتمي).
- [x] معالج الأعطال (Minidump + SEH).
- [x] مؤثرات ما بعد المعالجة (Vignette + Saturation) واهتزاز الكاميرا.
- [x] نافذة Profiler حية في المحرر + تصدير Chrome Trace.
- [x] نظام Quest/Inventory/Dialogue وGameplay Tags.
- [x] شخصيات متحركة فيزيائياً (Character Controller) وIK تحليلي.
- [x] ذكاء اصطناعي: A* شبكي + Behavior Trees.
- [x] جزيئات حتمية (Particles) وشبكات موثوقة (Reliable + Snapshots).
- [x] ربط ملفات الصوت بالمشاهد (`buffer=` يُفك تلقائياً).

---

## آلية المشاركة والمساهمة

1. اختيار إحدى النقاط المفتوحة في الجدول أعلاه أو في قسم الـ [Issues](https://github.com/abdallah2183/SANAD-Engine/issues).
2. كتابة تعليق في الـ Issue لتأكيد رغبتك بالعمل عليها لتجنب تكرار الجهود.
3. البدء في التنفيذ ومشاركة التقدم مع المؤسس عبدالله ومجتمع المطورين للمراجعة والدمج.
