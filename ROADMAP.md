# خارطة طريق محرك سَنَد | SANAD Development Roadmap

توضح هذه الوثيقة مراحل بناء وتطوير **محرك سَنَد (SANAD Engine)** بقيادة وتأسيس **عبدالله**، حيث تبين ما تم إنجازه واختباره والمراحل الجارية والمستقبلية المفتوحة للمساهمة من المطورين.

---

## جدول المراحل البرمجية

| المرحلة (Phase) | الوصف | الحالة | المجالات المفتوحة للمساهمة |
| :--- | :--- | :---: | :--- |
| **Phase 1–3** | النواة الأساسية، RHI Vulkan، نظام الوظائف Jobs، معمارية ECS | **مكتمل** | صيانة وتحسين الأداء |
| **Phase 4–6** | محرر ImGui، التفتيش التفاعلي، مستعرض الأصول، التراجع والإعادة | **مكتمل** | تحسينات تجربة المستخدم |
| **Phase 7–8** | خط أنابيب الإضاءة المؤجلة PBR، RenderGraph، مواد وخامات | **مكتمل** | مصفوفات المواد |
| **Phase 9–10** | محرك الفيزياء الحتمي، نظام التحريك الهيكلي، صوتيات 3D | **مكتمل** | إضافة أشكال تصادم إضافية |
| **Phase 11** | استقلالية الطبقات، نظام الحفظ المتقدم، معمارية بث العوالم | **مكتمل** | اختبارات الاستقرار |
| **Phase 12** | مصفوفات تفاصيل النماذج (Dynamic Mesh LOD & Mesh Streaming) | **مكتمل** | خوارزميات التبسيط |
| **Phase 13** | خرائط الظلال الاتجاهية (Directional Shadows) وسماء إجرائية | **مكتمل** | تظليل الشيدرز، PCF |
| **Phase 14** | دمج MiniAudio للصوتيات واستيراد ملفات glTF 2.0 | **مكتمل** | استيراد الأصول، صيغ الصوت |
| **Phase 15** | ربط لغات البرمجة (C# / Lua) ودعم الخط العربي RTL في المحرر | **مكتمل** | بيئات التشغيل، خطوط HarfBuzz |
| **Phase 16** | محرك الفيزياء المتقدم Jolt Physics وشبكات اللعب المتعدد | **مكتمل** | عرض الوسائط |

---

## تفاصيل المراحل القادمة

### المرحلة 13: الظلال والسماء الإجرائية (Directional Shadows & Sky)
- [x] إضافة هدف ظلال بدقة 2048×2048 بصيغة `D32_SFloat`.
- [x] ممر ظلال اتجاهي عبر RenderGraph مع إسقاط متعامد (Orthographic Light Projection).
- [x] مرشح تنعيم الظلال 3×3 PCF مع موازنة الظلال (Shadow Bias).
- [x] سماء إجرائية متدرجة مع قرص الشمس دون الحاجة لخامات جاهزة (SkyParams + قرص/توهج قابل للضبط).
- [x] عناصر تحكم في المحرر لتفعيل الظلال وضبط شدتها (قوة الظل، الانحياز، قسم Sky كامل).
- [x] إعدادات السماء والظلال محفوظة في صيغة المشهد (`.nfscene`: سطر `Sky:` ومفاتيح `shadow_strength/bias`).

### المرحلة 14: الصوتيات واستيراد النماذج (Audio & glTF)
- [x] خط استيراد صوتي حقيقي: فك ترميز `WAV` و`OGG` و`MP3` و`FLAC` إلى `AudioBuffer` (miniaudio + stb_vorbis).
- [x] تحويل القنوات/معدل العينات + كشف الصيغة من البصمة السحرية.
- [x] بناء أداة استيراد لنماذج `glTF 2.0` (`.gltf`/`.glb`) وتحويلها إلى صيغة المحرك `.nfmesh` (أداة `NFModelImporter`).

### المرحلة 12: تفاصيل النماذج (LOD)
- [x] مولد LOD حتمي (تجميع شبكي لكل SubMesh) + بناء مستويات تلقائي ببحث ثنائي.
- [x] ربط الاختيار المسافي الموجود (`select_lod`) بمستويات مولدة ومختبرة.

### المرحلة 15: لغات البرمجة والتعريب (Scripting & Arabic UI)
- [x] دعم لغة **Lua 5.4** لكتابة منطق الألعاب (VM معزولة، مكتبة `nf.*`، ربط الكيانات، نظام `ScriptComponent`).
- [x] دعم تشكيل النصوص العربية (أشكال سياقية + ligatures + ترتيب RTL) داخل واجهة المحرر Dear ImGui (خط Amiri مدمج).
- [x] ترجمة واجهة المحرر (القوائم، النوافذ، الأقسام) إلى اللغة العربية مع زر تبديل فوري.

### المرحلة 16: فيزياء متطورة وشبكات ألعاب (Advanced Physics & Multiplayer)
- [x] طبقة نقل UDP غير حاجبة + قناة موثوقة (تسلسل/إقرار/إعادة إرسال) + لقطات حالة مُصدَرة.
- [x] سيرفر Authoritative بمحاكاة حركية مشتركة + تنبؤ العميل + مصالحة (مختبرة على loopback حقيقي).
- [x] دمج محرك الفيزياء العالمي **Jolt v5.6** (مكتبة static حتمية) + غلاف JoltWorld (كرة/صندوق/مستوى، سقوط/تكديس/حتمية مختبرة).
- [x] مركبات Jolt ونسخ القيود المعقدة (Jolt جاهز لها: Vehicle/Ragdoll/Constraints مبنية).
- [x] استعلامات المشهد على Jolt: Raycast + Sphere Sweep + استعلامات التراكب (Overlap Sphere/Box).
- [x] مستشعرات/مرسيات (Triggers/Sensors) لا تحجز الحركة + كشف التراكب.
- [x] تصادم مستمر (CCD / Linear Cast) للأجسام السريعة.
- [x] متحكم الشخصية على Jolt (§39): ارتفاع الدرجة (Step Offset) والمنحدرات، منصات متحركة، انحناء (Crouch)، وخطاف تسلق (Climb Hook)، مع حتمية التنبؤ الشبكي (CharacterVirtual).
- [x] أشكال تصادم معقّدة على Jolt: كبسولة (Capsule) + محدب (Convex Hull) + شبكة ساكنة (Static Mesh).
- [x] **نسخ القيود** (`clone_constraint`): استنساخ أي قيد بين جسمين جديدين مع حفظ النوع والحدود والمحاور — لبناء سلاسل/أراجيح/هياكل متكررة من قالب واحد.
- [x] **حالات العجلات وإعادة التعيين**: قراءة حالة كل عجلة (التماس، انضغاط التعليق، الدوران، الزاوية) لعرض النماذج، و`vehicle_reset` لنقل المركبة وتصفير سرعتها دون إعادة بنائها.
- [x] **نسخ المركبات والقيود على الشبكة**: مدخلات قيادة مشفرة (`VehicleDriveInput` بـ20 بايت عبر القناة الموثوقة)، لقطات هيكل دورية (`VehicleSnapshot` بسحر `NFVS` عبر UDP خام)، وأحداث مفاصل مرتبة (`ConstraintEvent` بسحر `NFCE`: إنشاء/حذف/نسخ لكل أنواع القيود السبع) — مع طابور مدخلات للسيرفر وأشباح Authoritative للعميل (مختبرة على loopback حقيقي).
- [x] **ربط C# عبر استضافة .NET 10** (`CSharpHost` بـnethost/hostfxr + صندوق `NFGameScript` المُدار): دورة Start/Update/Counter لكل كيان، استدعاءات راجعة للمضيف، وأنواع blittable فقط عبر الحدود — تهيئة واحدة لكل عملية (التفريغ يسمم إعادة التهيئة)، واختبارات الاستضافة تتخطى نفسها (NF_SKIP) عند غياب الرانتايم.
- [x] قماش/أجسام ناعمة (Cloth/Soft Body): شبكة جزيئات PBD بقيود هيكلية/قص/انحناء + تصادم مع كرة/صندوق/مستوى (محلّل داخلي).

### إضافات خارج الخارطة (مكتملة)
- [x] دورة الليل والنهار (Time-of-Day: شمس وسماء ديناميكية).
- [x] توليد الأراضي الإجرائية (Heightfield + value-noise حتمي).
- [x] تسجيل وإعادة تشغيل المدخلات (Replay حتمي).
- [x] معالج الأعطال (Minidump + SEH).
- [x] مؤثرات ما بعد المعالجة (Vignette + Saturation) واهتزاز الكاميرا.
- [x] نافذة Profiler حية في المحرر + تصدير Chrome Trace.
- [x] نظام Quest/Inventory/Dialogue وGameplay Tags.
- [x] شخصيات متحركة فيزيائياً (Character Controller) وIK تحليلي.
- [x] ذكاء اصطناعي: A* شبكي + Behavior Trees.
- [x] جزيئات حتمية (Particles) وشبكات موثوقة (Reliable + Snapshots).
- [x] ربط ملفات الصوت بالمشاهد (`buffer=` يُفك تلقائياً).

---

## آلية المشاركة والمساهمة

1. اختيار إحدى النقاط المفتوحة في الجدول أعلاه أو في قسم الـ [Issues](https://github.com/abdallah2183/SANAD-Engine/issues).
2. كتابة تعليق في الـ Issue لتأكيد رغبتك بالعمل عليها لتجنب تكرار الجهود.
3. البدء في التنفيذ ومشاركة التقدم مع المؤسس عبدالله ومجتمع المطورين للمراجعة والدمج.
