# خارطة طريق محرك سَنَد | SANAD Development Roadmap

توضح هذه الوثيقة مراحل بناء وتطوير **محرك سَنَد (SANAD Engine)** بقيادة وتأسيس **عبدالله**، حيث تبين ما تم إنجازه واختباره والمراحل الجارية والمستقبلية المفتوحة للمساهمة من المطورين.

---

## جدول المراحل البرمجية

| المرحلة (Phase) | الوصف | الحالة | المجالات المفتوحة للمساهمة |
| :--- | :--- | :---: | :--- |
| **Phase 1–3** | النواة الأساسية، RHI Vulkan، نظام الوظائف Jobs، معمارية ECS | **مكتمل** | صيانة وتحسين الأداء |
| **Phase 4–6** | محرر ImGui، التفتيش التفاعلي، مستعرض الأصول، التراجع والإعادة | **مكتمل** | تحسينات تجربة المستخدم |
| **Phase 7–8** | خط أنابيب الإضاءة المؤجلة PBR، RenderGraph، مواد وخامات | **مكتمل** | مصفوفات المواد |
| **Phase 9–10** | محرك الفيزياء الحتمي، نظام التحريك الهيكلي، صوتيات 3D | **مكتمل** | إضافة أشكال تصادم إضافية |
| **Phase 11** | استقلالية الطبقات، نظام الحفظ المتقدم، معمارية بث العوالم | **مكتمل** | اختبارات الاستقرار |
| **Phase 12** | مصفوفات تفاصيل النماذج (Dynamic Mesh LOD & Mesh Streaming) | **مكتمل** | خوارزميات التبسيط |
| **Phase 13** | خرائط الظلال الاتجاهية (Directional Shadows) وسماء إجرائية | **مكتمل** | تظليل الشيدرز، PCF |
| **Phase 14** | دمج MiniAudio للصوتيات واستيراد ملفات glTF 2.0 | **مكتمل** | استيراد الأصول، صيغ الصوت |
| **Phase 15** | ربط لغات البرمجة (C# / Lua) ودعم الخط العربي RTL في المحرر | **مكتمل** | بيئات التشغيل، خطوط HarfBuzz |
| **Phase 16** | محرك الفيزياء المتقدم Jolt Physics وشبكات اللعب المتعدد | **مكتمل** | عرض الوسائط |
| **Phase 17** | نسخ الأجسام والمفاصل على الشبكة (اتجاه المركبات، سجل الأجسام، مطبق القيود) | **مكتمل** | عرض الوسائط |
| **Phase 18** | طبقة ثنائية الأبعاد مستقلة (`NFScene2D`: أطلس، دفعة، خريطة بلاط، فيزياء 2D حتمية) | **مكتمل** | ظلال نقاطية، Bloom/SSAO |
| **Phase 19** | التدمير (`NFDestruction`: هندسة الكسر، أصول الشظايا، عالم الضرر، ميزانيات الحطام، `IDebrisSink` عبر Jolt) | **مكتمل** | تدمير الشبكات الحية من ملف المشهد |
| **Phase 20** | الظلال المتتالية المربوطة بالكاميرا (Cascaded Shadow Maps: تقسيم Lauritzen، أطلس 2×2، تثبيت التكسل، انحياز مشتق، مزج تسليم) | **مكتمل** | ظلال نقاطية |
| **Phase 22** | نواة الذكاء الاصطناعي: إدراك (Perception) بأربع حواس مُحقن فيها الحجب، آلات حالة حتمية، وذكاء فائدة بمنحنيات استجابة | **مكتمل** | NavMesh/Recast، عقود الذكاء الاصطناعي للعوالم المفتوحة |

---

## تفاصيل المراحل القادمة

### المرحلة 13: الظلال والسماء الإجرائية (Directional Shadows & Sky)
- [x] إضافة هدف ظلال بدقة 2048×2048 بصيغة `D32_SFloat`.
- [x] ممر ظلال اتجاهي عبر RenderGraph مع إسقاط متعامد (Orthographic Light Projection).
- [x] مرشح تنعيم الظلال 3×3 PCF مع موازنة الظلال (Shadow Bias).
- [x] سماء إجرائية متدرجة مع قرص الشمس دون الحاجة لخامات جاهزة (SkyParams + قرص/توهج قابل للضبط).
- [x] عناصر تحكم في المحرر لتفعيل الظلال وضبط شدتها (قوة الظل، الانحياز، قسم Sky كامل).
- [x] إعدادات السماء والظلال محفوظة في صيغة المشهد (`.nfscene`: سطر `Sky:` ومفاتيح `shadow_strength/bias`).

### المرحلة 14: الصوتيات واستيراد النماذج (Audio & glTF)
- [x] خط استيراد صوتي حقيقي: فك ترميز `WAV` و`OGG` و`MP3` و`FLAC` إلى `AudioBuffer` (miniaudio + stb_vorbis).
- [x] تحويل القنوات/معدل العينات + كشف الصيغة من البصمة السحرية.
- [x] بناء أداة استيراد لنماذج `glTF 2.0` (`.gltf`/`.glb`) وتحويلها إلى صيغة المحرك `.nfmesh` (أداة `NFModelImporter`).

### المرحلة 12: تفاصيل النماذج (LOD)
- [x] مولد LOD حتمي (تجميع شبكي لكل SubMesh) + بناء مستويات تلقائي ببحث ثنائي.
- [x] ربط الاختيار المسافي الموجود (`select_lod`) بمستويات مولدة ومختبرة.

### المرحلة 15: لغات البرمجة والتعريب (Scripting & Arabic UI)
- [x] دعم لغة **Lua 5.4** لكتابة منطق الألعاب (VM معزولة، مكتبة `nf.*`، ربط الكيانات، نظام `ScriptComponent`).
- [x] دعم تشكيل النصوص العربية (أشكال سياقية + ligatures + ترتيب RTL) داخل واجهة المحرر Dear ImGui (خط Amiri مدمج).
- [x] ترجمة واجهة المحرر (القوائم، النوافذ، الأقسام) إلى اللغة العربية مع زر تبديل فوري.

### المرحلة 16: فيزياء متطورة وشبكات ألعاب (Advanced Physics & Multiplayer)
- [x] طبقة نقل UDP غير حاجبة + قناة موثوقة (تسلسل/إقرار/إعادة إرسال) + لقطات حالة مُصدَرة.
- [x] سيرفر Authoritative بمحاكاة حركية مشتركة + تنبؤ العميل + مصالحة (مختبرة على loopback حقيقي).
- [x] دمج محرك الفيزياء العالمي **Jolt v5.6** (مكتبة static حتمية) + غلاف JoltWorld (كرة/صندوق/مستوى، سقوط/تكديس/حتمية مختبرة).
- [x] مركبات Jolt ونسخ القيود المعقدة (Jolt جاهز لها: Vehicle/Ragdoll/Constraints مبنية).
- [x] استعلامات المشهد على Jolt: Raycast + Sphere Sweep + استعلامات التراكب (Overlap Sphere/Box).
- [x] مستشعرات/مرسيات (Triggers/Sensors) لا تحجز الحركة + كشف التراكب.
- [x] تصادم مستمر (CCD / Linear Cast) للأجسام السريعة.
- [x] متحكم الشخصية على Jolt (§39): ارتفاع الدرجة (Step Offset) والمنحدرات، منصات متحركة، انحناء (Crouch)، وخطاف تسلق (Climb Hook)، مع حتمية التنبؤ الشبكي (CharacterVirtual).
- [x] أشكال تصادم معقّدة على Jolt: كبسولة (Capsule) + أسطوانة (Cylinder) + محدب (Convex Hull) + شبكة ساكنة (Static Mesh).
- [x] **مخطط الارتفاعات (Height Field)** (`add_heightfield_body`): شبكة `sample_count²` صفّ-رئيسي بنفس اصطلاح أرض المحرك (شبكة على XZ، +Y أعلى) — `offset + scale*(x, heights[y*n+x], y)`، فيسّ التضاريس الذي يُرسَم بلا مصادم. يُفرَض ساكناً مثل الشبكة (`MustBeStatic`)، والمُدخلات الفاسدة (عدد عينات < 4، حجم لا يطابق الشبكة، NaN، مقياس X/Z صفري) تُرجع مقبضاً غير صالح دون إضافة جسم أو تبديل الشكل.
- [x] **الجسم المركّب (Compound)** (`add_compound_body`): قائمة من الأجزاء (`CompoundPart`: `CompoundShape` كرة/صندوق/أسطوانة/كبسولة + موضع واتجاه محليان) تُجمَع في شكل واحد — قعره ثابت (`StaticCompoundShape`) فلا يُعاد ترتيب أجزائه أبداً، لكن الجسم نفسه حرّ ديناميكي (عكس الشبكة/مخطط الارتفاعات الذي `MustBeStatic`). مفردة الأشكال هذه خاصة بالأجزاء لا يُعاد استخدام `Shape` الأول: ذاك مغلق عن قصد على كرة/صندوق/مستوى لأن المحلّل الضيق يتبدّل على نوعه والفضاء العريض يقرأ مداه، بينما الجزء يصل إلى Jolt وحده ولا يصل المحلّل أبداً — فتوسيعه ليشمل الأسطوانة والكبسولة هو بالضبط ما يسمح بعجلة على محور وبرميل داخل جسم واحد. يملأ الفجوة بين النوعين: الهيكل المحدب مَقعَّرُ مستحيل (مُحدَّبٌ بنيانه)، والشبكة ساكنة لا تتحرك — فالطاولة بأرجلها والمفصليات والأجسام المَقعّرة المتحركة كلها مركّب. `state().position` يُطابق أصل فضاء الأجزاء كما كل شكل آخر هنا. أقل من جزئين، أو جزءٌ هندستُه منحطّة (نصف قطر ≤ 0، نصف امتداد ≤ 0، أو NaN)، يُرجع مقبضاً غير صالح — تُرفَض هندسة الجزء المنحطّ ولا تُجمَّع كما يفعل `Shape` الأول: الجزء يصل إلى Jolt وحده، وباني الأسطوانة عنده لا يُطبّع نصف قطر سالب. الاستثناء الوحيد كبسولة بنصف ارتفاع صفر فهي كرة عند Jolt. لا تبديل صامت لشكل آخر، ولا سقوط على شكل بديل.
- [x] **الأسطوانة (Cylinder)** (`add_cylinder_body`): نصف ارتفاع + نصف قطر، محورها Y كاعتبار Jolt. محدّبة فتتحرك ديناميكياً (برميل، أنبوب، عجلة بلا نظام المركبات) — تُثبَّت فوق صندوق بنفس الأبعاد فتستقران على نفس الارتفاع تماماً (أصل كل منهما عند قاعه)، لكن وجه الأسطوانة دائرةٌ مكتوبةٌ داخل مربع الصندوق: جسمٌ ساقطٌ في زاوية المربع يخترق الأسطوانة ويستقر على الصندوق، وشعاعٌ في الزاوية نفسها يصدم الصندوق ويمرّ من الأسطوانة. نصف قطر ≤ 0 أو نصف ارتفاع ≤ 0 (والـ NaN معهما) يُرجع مقبضاً غير صالح — Jolt يثبت نصف قطر الكرة ولكنه لا يثبت الأسطوانة، فالعقد هو ما يحرسها.
- [x] **نسخ القيود** (`clone_constraint`): استنساخ أي قيد بين جسمين جديدين مع حفظ النوع والحدود والمحاور — لبناء سلاسل/أراجيح/هياكل متكررة من قالب واحد.
- [x] **حالات العجلات وإعادة التعيين**: قراءة حالة كل عجلة (التماس، انضغاط التعليق، الدوران، الزاوية) لعرض النماذج، و`vehicle_reset` لنقل المركبة وتصفير سرعتها دون إعادة بنائها.
- [x] **نسخ المركبات والقيود على الشبكة**: مدخلات قيادة مشفرة (`VehicleDriveInput` بـ20 بايت عبر القناة الموثوقة)، لقطات هيكل دورية (`VehicleSnapshot` بسحر `NFVS` عبر UDP خام)، وأحداث مفاصل مرتبة (`ConstraintEvent` بسحر `NFCE`: إنشاء/حذف/نسخ لكل أنواع القيود السبع) — مع طابور مدخلات للسيرفر وأشباح Authoritative للعميل (مختبرة على loopback حقيقي).
- [x] **ربط C# عبر استضافة .NET 10** (`CSharpHost` بـnethost/hostfxr + صندوق `NFGameScript` المُدار): دورة Start/Update/Counter لكل كيان، استدعاءات راجعة للمضيف، وأنواع blittable فقط عبر الحدود — تهيئة واحدة لكل عملية (التفريغ يسمم إعادة التهيئة)، واختبارات الاستضافة تتخطى نفسها (NF_SKIP) عند غياب الرانتايم.
- [x] قماش/أجسام ناعمة (Cloth/Soft Body): شبكة جزيئات PBD بقيود هيكلية/قص/انحناء + تصادم مع كرة/صندوق/مستوى (محلّل داخلي).

### المرحلة 17: نسخ الأجسام والمفاصل على الشبكة (Body & Joint Replication)- [x] اتجاه المركبات في اللقطات (`NFVS` v2 بسجلات 44 بايت: quaternion وحدة مرفوض غيرها + `last_angle_correction` للأشباح) — الشبح الذي يتحرك بلا اتجاه يرسم سيارة تنزلق جانباً.
- [x] الاتجاه في `JoltBodyState` (ذري مع الموضع) وكتابته في `Transform` عبر `VehicleSystem` + `reset` يعيد الاستواء (صفر euler).
- [x] أحداث الأجسام (`BodyEvent` بسحر `NFBE`: إنشاء/حذف بتحقق صارم) + سجل `BodyRegistry` بأولوية الهرمية (static scenery لا يُقتلع).
- [x] المطبق `ConstraintApplier`: ترتيب (أجسام قبل مفاصل مع تأجيل)، idempotency، teardown المفاصل اليتيمة عند حذف/استبدال جسم، و`clone_constraint over the wire` end-to-end (مختبر بعوالم وهمية + قناة موثوقة حقيقية).
- [x] **إصلاح انقلاب الصورة (Vulkan Y-flip)**: الإسقاط كان OpenGL-style بدون قلب Y — كل إطار يُرسم مقلوباً عمودياً (يثبت بالرياضيات واللقطات: السيارة فوق عجلاتها). الإصلاح: `m[1][1]` سالبة في perspective/orthographic + `FrontFace::CW` لخطوط العمق/GBuffer/الالتقاط، مع اختبار `test_mat4_perspective_points_up_at_negative_ndc_y` الذي يمنع الانتكاس.
- [x] **ديمو قيادة مركبة** (`NFSampleVehicleDemo`): سيارة Jolt حقيقية بعجلات مرئية (موضع/توجيه/دوران من `wheel_states`) وكاميرا تتبع وبوابات ذهبية وصناديق قابلة للتحطيم — WASD للقيادة وSpace للفرامل وR لإعادة التعيين، مع لقطات موقع حقيقية.

### المرحلة 19: التدمير (Destruction — damage, fracture propagation, debris)
- [x] **فصل الطبقات عبر `IDebrisSink`**: `NFDestruction` حسابي بحت لا يلمس Jolt ولا GPU (الميزانية، تراكم الإجهاد، كينماتيكا الشظايا تُقرّر كلها بلا خلفية فيزيائية) ⇒ يُختبر على أي جهاز. `JoltDebrisSink` في `NFPhysics` هو التنفيذ الذي يحوّل `DebrisSpawn` إلى جسم Jolt محدّب — التبعية تسير باتجاه واحد فقط.
- [x] **عالم الضرر (`DestructionWorld`)**: تساقط خطي للمقدار، إجهاد متراكم على الروابط، وفصل الشجرة الفرعية عن الجزء المنفصل.
- [x] **تقسيم الحجم بدقة**: القطع المنبعثة تقسم المنطقة المنفصلة تماماً — لا تداخل ولا فجوة ولا انبعاث مزدوج (القطعة الواحدة تساوي اتحاد أحجام أبنائها ما دام رابطها الداخلي سالماً).
- [x] **الميزانية داخل `apply_damage` لا بعدها**: حد أقصى للروابط المكسورة لكل إطار (الضرر يؤجّل ولا يضيع)، سقف صارم للشظايا الحية يُقاعد الأقدم أولاً، وعمر أقصى. قنبلة في مستودع صناديق تُكبَّح بالتصميم لا بالضبط اللاحق.
- [x] **عدم حتمية كعقد**: لا RNG، لا ساعة، لا حاوية يعتمد ترتيبها على قيم المؤشرات؛ الروابط بترتيب الأصل، الشظايا بترتيب الشجرة، التقاعد بالعمر ثم بالوصول.
- [x] **`JoltDebrisSink`**: يعقّب مجموعه الحي الخاص لا `body_count()` العام — الميزانية سقف على *الحطام*، والعالم يحوي الأرضية والمركبات واللاعب أيضاً.
- [x] **`JoltWorld::set_angular_velocity`**: دوران الشظية يأتي من هبوط الدفعة بعيداً عن المركز؛ إسقاطه يجعلها تسقط مسطّحة.
- [x] **موضع الشظية = أصل الأصل**: `spawn.position` كان يعطي *المركز الهندسي* للقطعة بينما الهيكل نفسه في فضاء الأصل — فتُوضع كل شظية منحازة بمقدار مركزها المحلي (طريقة فخّ مركز الكتلة نفسها: لا أثر لها لأي شكل متمركز، فلا اختبار فيزيائي يكشفها). الآن الموضع هو أصل الأصل في العالم، فيطابق `hull_points` مكان القطعة الأصلي تماماً.
- [x] **دمج التدمير في `Runtime` (القاعدة 0)**: `enable_destruction` يبني عالم Jolt مخصصاً للحطام + `JoltDebrisSink` + `DestructionWorld`، ويضيف المصادمات الساكنة للمشهد حتى تستقر الشظايا على الأرضية لا تخترقها. `update()` يخطوها على ساعة ثابتة؛ وتغيير المشهد يُقاعد شظايا المشهد السابق. `RuntimeTests/test_runtime_destruction.cpp` هو الملف الذي يثبت ذلك: حذف `step_destruction` من `update()` يجعل اختباره تفشل (تُولد الشظايا ولا تسقط).
- [x] **رسم الشظايا (القاعدة 0)**: الشظية كانت تُحاكى ولا تُرسم — `debris_samples()` يستهلكه اختبار فقط. الآن `build_debris_render_world()` تُستدعى من داخل `build_render_world()`، أي نفس العالم الذي يستهلكه `render()`، فالشظية تُرسم في الإطار نفسه. شبكتها تُبنى من رؤوس القطعة نفسها وتُخزّن cached لكل زوج (أصل، قطعة)؛ و`chunk_of()` في الحوض مع فرق `live_ids()` حول `apply_damage()` يربط كل شظية بأصلها دون أن يتعلم `NFDestruction` شيئاً عن جدول الأصول. `render_world_snapshot()` جديد للاختبارات، والاختبار يفشل فعلاً عند حذف الاستدعاء (مُتحقق بالتجربة).
- [x] **تركيب وضع الشظية**: `rotation * translate` لا العكس — مصفوفات المحرك شعاع-صفية (`v * M`) فيُطبَّق العامل الأيمن أولاً، فالترجمة على اليسار تدور *موضع* الجسم حول أصل العالم أيضاً. الشظية الهادئة قرب الأصل لا تكاد تظهر (المحرف مليمترات) والشظية المتقلبة على بُعد 95 وحدة تهبط في مكان آخر تماماً. `compose_trs_mat4` و`Skeleton` يستخدمان الترتيب الصحيح.
- [x] **التدمير من مشهد (القاعدة 0 — إغلاق فجوة الوصول)**: كل سلسلة Phase 19 (`enable_destruction` ← `register_fracture_asset` ← `bind_destructible` ← `apply_damage` ← `step_destruction` ← الرسم) كانت لا تُستدعى إلا من اختبار: لا عيّنة ولا محرّر ولا نقطة دخول لعبة تلمسها. الآن `DestructibleComponent` في المشهد يصف الجسم القابل للكسر (`chunks`/`seed`/`strength`/`damage_threshold`/`blast_radius`/`enabled`)، فيطبخه `RuntimeSceneLoader` نصياً ذهاباً وإياباً (الخيار اختياري تماماً لمن لا يستخدمه)، و`bind_scene_destructibles()` تُستدعى من `adopt_scene()` فتُطبخ `FractureAsset` من المصادم الصندوقي وتُسجّله وتربطه وتُفعّل التدمير عند أول جسم قابل — لا يذكره كود اللعبة. `step_impact_damage()` تُستدعى من `update()` بعد الفيزياء، تقرأ `last_manifolds()` فوراً فتطابق الجهة المصدومة عبر `RigidBodyComponent::body`، تجمع `normal_impulse` (مقدار الاصطدام الحقيقي، لا وزن السكون)، وتطبّق الضرر عند تجاوز العتبة. الكسر يعيد الجسم: `retire_shattered_entity()` تحذف جسم Jolt *قبل* `destroy_entity` (لو بقيت ساككة صندوق غير مرئي) ثم تبني المصادمات الساكنة للحطام من جديد حتى لا تستقر الشظايا على الصندوق الذي قُطعت منه. الحتمية محفوظة رغم أن `m_destructibles` خريطة غير مرتبة: قائمة التقاعد تُفرز قبل التنفيذ. `Content/Scenes/Destruction.nfscene` يعرض ذلك (صندوق + كرة هابطة)، و`Tests/RuntimeTests/test_runtime_scene_destruction.cpp` يثبته عبر `adopt_scene` + `update()` فقط — لا شيء يستدعي واجهة التدمير مباشرة.
- [x] جناحا `DestructionTests` 48/48 و `RuntimeTests` 58 اختباراً (52 ناجحاً). (الـ6 الفاشلة كلها في `rhi::validation_error_count()` ورسم البكسل نتيجة إعادة صياغة `Renderer3D.cpp`/`forward.frag`/`gbuffer.frag` الجارية على آلة أخرى — لا علاقة لها بهذا الطور، وشفيتي مُعدّمتان لأي مشهد بلا `DestructibleComponent`. `PhysicsTests` صار 202/202 بعد اكتمال `add_compound_body`.)

### المرحلة 20: الظلال المتتالية المربوطة بالكاميرا (Cascaded Shadow Maps)
- [x] **إصلاح الثغرة الجوهرية**: مصفوفة الظل كانت صندوقاً متعامداً `orthographic(-12,12,-12,12,1,60)` **مثبّتاً حول نقطة الأصل العالمية** والضوء عند `dir * -20`. النتيجة: أي كاميرا تبعد أكثر من ~12 وحدة عن الأصل تفقد ظلالها، وأي عالم أكبر من 24×24 وحدة لا ظل فيه إطلاقاً. الآن كل شريحة تُركَّب على زوايا frustum الكاميرا الحقيقية ⇒ الظلال تعمل في أي موضع من العالم.
- [x] **أطلس 2×2 بدل مصفوفة طبقات**: `VulkanFramebuffer` يأخذ `Texture*` ويستعمل عرضاً أحادي الطبقة، فدعم `View2DArray` كان سيتطلب تغيير RHI. الأطلس 2048×2048 (`1024` لكل شريحة) = **نفس ذاكرة اليوم بالضبط**، لكن بأربع مدىات بدل واحدة، مع `set_viewport/set_scissor` لكل شريحة داخل ممر RenderGraph واحد ⇒ حاجز واحد.
- [x] **تقسيم عملي (Lauritzen)**: `d_i = λ·n·(f/n)^(i/N) + (1-λ)·(n+(f-n)·i/N)` — الدقة حيث يقرأها اللاعب. الشريحة قابلة للضبط 1..4، مع `shadow_distance` لتضييق المدى (0 = مستوى الكاميرا البعيد).
- [x] **فرض كرويّة الفروق (Texel Snapping)**: الصندوق يُكمَّم لمضاعفات كاملة من حجم التكسل، وإلا اهتزّت حدود الظل مع كل حركة كاميرا. اختبار يقيس صندوقاً يغطي الشريحة عند كل إزاحة كسرية من التكسل — البداية لأسفل والنهاية لأعلى، وإلا قُصّ شريط بعرض تكسل عند المواضع غير المحظوظة.
- [x] **انحياز مُشتقّ لكل شريحة**: الانحياز الثابت كان خطأً فيزيائياً بعد التتالي — الانحياز بوحدات NDC يشتري إزاحة عالمية = `bias × مدى العمق`، والتتالي قلّص الاثنين بنسب مختلفة، فظهرت حبوب الظل (Acne) على الأوجه المضيئة. الآن `cascade_auto_bias` = `(حجم التكسل × tan θ) ÷ مدى العمق` لكل شريحة، **يُضاف** فوق `shadow_bias` الذي يضبطه الفنان ⇒ معناه ومداه `[0, 0.01]` لم يتغيّرا.
- [x] **مزج تسليم الشريحة (Cross-fade)**: تسليم صلب بين شريحتين بكثافتين مختلفتين يظهر كخط تتغيّر عنده حدة حدّ الظل؛ المزج داخل `fade_range` يخفيه، مع قصّ كل نقرة PCF داخل حدود شريحتها لمنع تسرّب الظل بين الشرائح.
- [x] **تحقق بلا GPU**: `ShadowCascades.hpp/.cpp` رياضيات خالصة (تقسيم، تركيب، تكسيل، اختيار شريحة، انحياز) بلا جهاز ⇒ تُختبر على أي آلة.
- [x] **اختبارات بكسل تكشف الانحدار بدقة**: صيغة تامة تساوي (`with_shadows == without` حرفياً) على وجه مضاء غير محجوب، وصفر تظليل ذاتي على أرضية بلا رامية — التصنيف الوحيد غير الملتبس بين حبوب الأرضية وظل المكعب. واختبار طاقة يثبت أن التتالي **يعيد توزيع** الظل دون أن يفقد منه شيئاً.
- [x] إعدادات متتالية في المشهد (`.nfscene`: `shadow_cascades`, `shadow_distance`) + شرائح في المفتش بتحقق وتراجع + مفاتيح تعريب `shadow_cascades`/`shadow_distance`.

### المرحلة 21: ظلال الأضواء المحلية (Point / Spot Shadows)
- [x] **أطلس واحد لكل البلاطات**: `kMaxShadowPointLights*6 + kMaxShadowSpotLights` = 28 بلاطاً في شبكة 7×7 (البلاط الافتراضي 512 بكسل ⇒ الحافة 3584، داخل حد Vulkan المضمون 4096). الضوء النقطي i يملك `[6i, 6i+6)` والشعاع j يملك البلاط `24+j` — نفس القاعدة على الـ CPU وفي الـ shader، وإعادة ترتيبها في مكان واحد فقط.
- [x] **المصفوفة هي المرجع، لا الوجه**: الـ shader يختار البلاط بالمحور السائد (كسر التعادل للأدنى، مطابقاً لـ `select_face` على الـ CPU) ولكنه يأخذ الـ NDC من المصفوفة المرفوعة ولا يعيد اشتقاق قاعدة الوجه — فلا يمكن لخطأ الإسناد أو الانعكاس أن يُنتج عمقاً "معقولاً" يختبئ في المنقح.
- [x] **PCF 3×3 مقصوصاً داخل البلاط**: كل نقرة تُقطّع إلى حدود بلاطها، لأن البلاط المجاور هو مسقط *آخر*، وقراءة عمقه تُسرب بقعة ظل لا علاقة لها عبر الدرز.
- [x] **انحياز مُشتق + انحياز الفنان**: مثل مسار التتالي تماماً — `local_shadow_auto_bias` لكل بلاط من حجم تكسله ومدى عمقه، ويُضاف فوق `shadow_bias` للضوء. مسار التتالي نفسه علّم أن الانحياز الثابت وحده يُظهر حبوب الظل بعد تغيّر المدى، فلا إعادة اختراع هنا.
- [x] **مدى الشعاع صار خاصية الضوء**: `lighting.frag` كان يحمل `float range = 25.0` حرفياً، و`kLocalShadowSpotDefaultFar` على الـ CPU يحمل الـ 25 نفسها — حرفان منفصلان لا رابط بينهما، فلا يمكن إعطاء الشعاع مدى قصيراً في غرفة صغيرة، ومسقط الظل كان يصرف دقة العمق حتى 25 وحدة مهما كان المشهد صغيراً. الآن `SpotLight::range` (افتراضه = تلك الـ 25، فكل مشهد موجود يُضاء ويُظلل بِت-بِت كما قبل — مُتحقق: مجاميع البكسلات في اختبارات الشعاع لم تتغير) يقرأه الـ shader عبر `SpotGPU::outer_range.y` (فتحة كانت حشواً، فلا تغيير في حجم الـ UBO ولا في الإزاحات) وهو نفسه خط الرجوع لمستوى البعد للمسقط.
- [x] **ربط المصوّر في المشهد**: عينة `Basic3D` تُظهر الميزة لا أن تقول إنها موجودة — ضوء نقطي يدور حول المشهد بشظيته الكاملة المرئية، وشعاع ثابت فوق رؤوس المكعبات (التصويب الوحيد الذي يُسقط `fit_spot` على +X كأعلى لأن +Y موازٍ لمحوره)، ومصباح باعث يتبع الضوء المتحرك. قائمة الأضواء دائمة عبر الإطارات، فالضوء المتحرك يُعدَّل في مكانه (`point_light(idx).position`) بدل إضافته كل إطار.
- [x] **اختبارات GPU تفصل الظل عن هندسته**: الرامي موجود في كل الإطارين المقارَنين، فكل بكسل مختلف هو الظل نفسه. أربع: نقطي يُظلّل، شعاعي يُظلّل، شعاع لأسفل على أرضية (يُثبت أن أساس `fit_spot` الساقط ليس NaN ولا معكوساً)، وصيغة **تساوي تامة بِت-بِت** لمشهد بلا رامي (اختبار حبوب الظل للمسار كله — التظليل الذاتي يكسر المساواة بصوت عالٍ، واختبار التغطية كان سيعدّ الحبوب كأنها "الظل يعمل"). خامسة لمدى الشعاع: إطاران يختلفان في حقل واحد فقط.

### المرحلة 22: نواة الذكاء الاصطناعي (Perception / State Machines / Utility AI)

المرحلة 21 في يد مطور آخر (`lighting.frag` + `Renderer3D`)، فكل عمل هذه المرحلة في ملفات جديدة لا يلمسها أحد — وكلها على الـ CPU، فلا حاجة لـ GPU أو Jolt أو ساعة لاختبار أي منها.

- [x] **الإدراك (`Perception`, §45 "perception")**: `PerceptionScene` هي ما عليه العالم (الأهداف + الأحداث العابرة)، و`Perception` هي حواس NPC واحد. أربع حواس (رؤية/سمع/لمس/ضرر) لكل منها `range`/`half_fov`/`threshold`/`gain`/`loss`/`linger`، وكشف مُ accumulative على مقياس 0..1 لكل حاسة — "رأيته نصف ثانية" ليس "رأيته".
  - **الحجب (occlusion) مُحقن لا مُتربص**: `set_sight_blocker(std::function<bool(Vec3,Vec3)>)`، فالوحدة تبقى CPU-only، و`NavGrid::has_line_of_sight` أو raycast فيزيائي كلاهما تنفيذ صالح بلا إعادة ترجمة. السمع يعبر الزوايا واللمس غير محجوب بالتعريف.
  - **موضع آخر معروف مُتدرّج الأولوية**: اللمس والرؤية يكتبان الموضع الحقيقي؛ ضجة مستمرة *لا* تكتب (الضجة تقول أين *كان*، والماشي غادر)؛ الحدث المنفصل (طلقة) يثبت موضعه. حجب البصر الأول فالرؤية تغطي ضجة قديمة في نفس الـ tick.
  - **`aware` مُغلَقة (latched)**: بمجرد امتلاء حاسة، يبقى اليقين حتى يُقلَل السطر — الفجوة بين التصريح باليقين ونزوله من العداد هي نافذة البحث بأكملها. إعادة حسابها كل tick كانت تجعل `linger` غير قابل للوصول.
  - **الاهتلاك فقط للحاسات الساكنة**: `loss` موثّقة على أنها "عند توقف المُحفّز"، فلا يُهتلك ما يصل حالاً. كان التهيئة الأصلية تصرف العداد *أثناء* وصول المُحفّز، فيصير المعدل (gain*stim − loss)، وأي محفز تحت loss/gain يستحيل أن يمتلئ أبداً — أي هدف مُتخفّي عن بعد يصير غير قابل للاكتشاف مهما طال التحديق (هذا التناقض بين التوثيق والتنفيذ كشفه التصميم لا الاختبار).
  - **الأثر المُتروك لا يُعاد دخوله**: `to == current` يُتخطّى لا يُعاد enter — وإلا دخل الهدف نفسه وخرج كل tick.
- [x] **آلة الحالة (`StateMachine`, §45 "state machines")**: `SMState` بـ enter/exit/tick، وانتقالات كحرّاس predicates على الـ Blackboard — الأسلاك بيانات، فالحالة نفسها تتألف differently تحت حارسين، والاختبار يثبت انتقالاً بلا عالم.
  - **tick قبل الحرّاس، انتقال واحد لكل tick**: الحارس قد يقرأ علماً رفعه الـ tick نفسه، فيُطلق في الـ tick الذي صار فيه شرطه صحيحاً لا tick متأخراً. سلسلة ثلاث حالات تأخذ ثلاث ticks — هذا ما يجعل السلسلة سلسلة لا قفزة.
  - **enter/exit يحيطان بالإقامة**: exit بعد tick وقبل enter التالي، فالحالة ترى enterها قبل tickها، ولا tick للحالة الجديدة في الـ tick الذي دخلت فيه.
  - **`force_state` للخارج لا للداخل**: صعق/استحواذ/سيناريو مكتوب — exit و enter يُدفعان، ويُعدّ انتقالاً (العداد لا يفرّق).
  - **تكديس مع Behavior Trees**: `SMStatus` و`BTStatus` نفس العدّادات، فحالة قد تُشغّل شجرة كاملة وتمرّر حالتها — طبقتان تتألفان بلا تحويل.
- [x] **ذكاء الفائدة (`UtilityAI`, §45 "utility AI")**: كل فعل يحمل `ResponseCurve` تحوّل رقم Blackboard واحد إلى رغبة 0..1، والنظام يسجّلها كلها ويُشغّل الفائز. المنحنيات هي سطح التأليف: "ما أشد رغبتي في الهرب عند 30% صحة" شكل لا فرع.
  - **منحنيات الأنواع الأربعة**: خطّي، تربيعي (المسافة لا تكاد تهم حتى تهم)، مقلوب بميل سالب + إزاحة (شبِع ثم نهِم)، وعتبي بميل كبير عند نقطة الكسر — كلها بحقول البنية الستة لا بكود.
  - **NaN يُسجَّل صفراً لا أن يُسمّم**: `x > NaN` كاذبة، فمقياس NaN يُسقط صاحبه *ويفسد* مقارنة من بعده؛ إكراهه على 0 يجعل السمّ مشكلة مُنتِجه لا خافيةً في النتيجة.
  - **التسجيل لا يكتب، التنفيذ وحده يكتب**: تسجيل الفائز ثم تنفيذه في تمريرة منفصلة — لو كتب التسجيل لاعتمدت قراءة الفعل التالي على ترتيب التقييم، وهو بالضبط ما يوجد قاعدة الترتيب لمنعه.
  - **كسر التعادل للأسبق على كل مستوى**: argmax بأخذ `>` صارم، فالاختيار دالة في المدخلات وحدها. `last_duration` يكشف التذبذب (فعلان يتقاطعان قربًا) كمشكلة ضبط لا تشغيل.
- [x] **عالم الذكاء الاصطناعي (`AIWorld`, §46 "AI World" + §47 "Crowd System")**: الوحدات فوقها تجيب "ماذا يقرر NPC واحد"، وهذه تجيب ما يجعل العالم المفتوح ممكناً أصلًا: *أي* NPCs يقررون، وكم مرة. شبيكة قطاعات ثابتة، و**تركيز** (focus) هو موضع يطلب ذكاءً كاملًا حوله (اللاعب، شريك co-op، أو طلقة رفعتها Perception للتو)، وقطاع يلامسه أي نصف قطر هو قطاع مرشّح — وما سواه يُحسم في تمرير مسح القطاعات ولا يدخل قائمة المرشحين أبدًا. هذا هو المنفذ: نصف العالم الهادئ لا يُكرَّر عليه أبدًا.
  - **المستوى والمَنحة محوران منفصلان**: المستوى (tier) هو *ما* يجري: RenderOnly / CheapMovement / LightweightAI / FullAI (مستويات §47 الأربعة). المَنحة (grant) هي *هل* يجري شيئٌ هذا الـ plan — والقيادة (steering) مستوى حقيقي يُمنح دائمًا ولا يُحاسب أبدًا. الخلط بينهما هو الخطأ المعتاد، وهو ينفصلان في مكان واحد بالضبط: ممثل أراد FullAI ولم تجد الميزانية خانة يُسجَّل CheapMovement لأن ذلك ما يفعله فعلًا.
  - **الانزياح (hysteresis) غير المتماثل ضد pop-in**: الترقية عند `d <= r` والتراجع يحتاج `d > r*(1+hysteresis)`، والمستوى السابق محفوظ عبر الـ plans (لا يُصفَّر)، فالممثل الواقف على حافة النطاق لا يقلب مستواه كل إطار — وهذا هو pop-in الذي يلاحظه اللاعب حتى حين يكون كل شيء آخر صحيحًا.
  - **الميزانية معدلات لا أعداد**: `full_ai_per_second` تتراكم على `dt` ويُصرف الجزء الصحيح ويحمل الكسر، فـ 2.5/ثانية تعطي 2، 3، 2، 3... لا 2 ثابتة بنصف ضائع كل إطار. لا دوران خانات — دورانها ينقضي قاعدة الأولوية؛ العدالة تأتي من الانزياح والكسر الحامل. والـ dt الضخم بعد توقّف التحميل لا يخزّن خلايا لانهائية: القصّ في فضاء الـ float قبل التحويل (التحويل خارج مدى u32 غير معرَّف أصلًا)، والفائض يُترك لا يُبنك.
  - **الممثلون المثبَّتون (`pinned_full`) خارج الميزانية والشبيكة معًا**: ذكاء المواجهة المكتوب لا يستهلك خانة ولا يخضع لقطاع لم يُلمس — الميزانية تحدّ *عالم* الممثلين، والنجوم ليست مشكلة العالم.
  - **كل مدخل مهمل يُصلَح عند الحافة لا في المسار الساخن**: dt سالب → صفر، معدلات سالبة → صفر، أنصاف أقطار غير مرتّبة → تُفرَز حتى تتداخل النطاقات (ثغرة بين مستويين مستحيلة)، شبيكة متحللة → تُرمَّم لـ 1×1 والوصول يُطالِع بما بُني فعلًا، وممثل خارج الشبيكة يُطوى على حافتها لا يُسقَط (إسقاطه يجعله غير مرئي للميزانية إلى الأبد).
- [x] **الحتمية عقداً موثّقاً في كل ملف**: ترتيب الإدراج هو ترتيب الزيارة ولا يُعاد فرزه؛ الروابط بالمعرفات لا المؤشرات ولا الـ hash؛ `dt` هو المدخل الزمني الوحيد ويُقصّب السالب إلى صفر؛ لا RNG ولا ساعة. `test_perception.cpp` و`test_state_machine.cpp` يثبتان إعادة تشغيلين متطابقتين بِت-بِت (مساواة `==` الحرفية على كل float). و`test_ai_world.cpp` يفعل الشيء نفسه على عالمين مستقلين لـ 20 plan متتالية — المستويات والمَنح والميزانية كلها متطابقة.
- [x] **النتيجة**: `AITests` من 14 اختباراً إلى **109** (30 إدراك + 16 آلة حالة + 21 فائدة + **28 عالم ذكاء اصطناعي** + 8 شجرة سلوك + 6 شبكة تنقل — الأربعة الأخيرة ملفات مسجّلة مسبقاً). `test_ai_world.cpp` مسجَّل في `Tests/CMakeLists.txt` ومُتأكَّد منه بالاسم في خرج التشغيل (28/28) — التسجيل الصامت الذي شُحن ثلاثاً لا يُكرَّر. (العدد الإجمالي الحالي عبر كل المجاميع في سطر الحالة في `.workbuddy-ai/memory/MEMORY.md` — لا يُكرَّر هنا لأنه يتغيّر مع كل طور.)

### المرحلة 23: تضاريس §59 — خرائط التغطية (Splat Maps) وخرائط الارتفاع (Heightmaps)

المرحلة 21 في يد مطور آخر (`lighting.frag` + `Renderer3D` + `RenderGraph`)، فكل عمل هذه المرحلة في `Engine/Rendering/Terrain.{hpp,cpp}` و`Samples/Basic3D` — وكلها على الـ CPU (باني الشبكة + الاستدعاءات + الاستيراد)، فلا حاجة لـ GPU لاختبار أي منها.

- [x] **خرائط التغطية (§59 "splat maps and layers")**: `build_terrain_mesh(options, layers)` — كل `TerrainLayer` بوابة ارتفاع + بوابة ميل (زاوية السطح عن +Y) + وزن، والباني يكتب `uv1 = (slot, blend)` لكل رأس. الطبقات تُكدَّس من الأرض لأعلى: القاعدة هي أصغر slot يغطي النقطة (الأرض التي يقف عليها)، والـ blend هو مقدار صعود السطح إلى الطبقة التي فوقها — فيصبح ترتيب قائمة الطبقات غير ذي تأثير، وطبقة رمل رقيقة تحت عشب كثيف تنحسر (blend 0.8 = 80% عشب) بدل أن يختفي العشب. يخلق الباني slot مادة لكل slot رسمه فعلاً.
  - **الميل من نفس нормال الإضاءة**: `slope = acos(n.y)` من الفرق المركزي الذي يستخدمه التظليل، فالبوابة التي يختارها الرأس هي البوابة التي يدل عليها تظليله.
- [x] **خرائط الارتفاع (§59 "heightmaps")**: `HeightField` — raster يُستدلى بدل حقل الضوضاء، يُبنى من `DecodedImage` عبر luma (Rec.601)، ويُعاين بمزدوج خطّي مع **حبس على الحواف** (العينة خارج الحقل تمدّ حدود الراستر ولا تُكرّر الجانب البعيد). `TerrainOptions::heightmap` مؤشر غير مملوك، وحين يكون فارغاً أو غير صالح يقع المسار القديم كأنه لم يحدث شيء — كل مستدعٍ قائم لا يتغير.
  - **نفس فضاء القياس لكلا المصدرين**: الحقل يُعاين في uv التضاريس ويُضرب في `height_scale` تماماً كالضوضاء، فيرى الـ splat نفس ارتفاعات العالم بأي طريقة، و`terrain_height` تُجيب خارج الباني (المكعبات والكاميرا في Basic3D تزرع نفسها على بيانات مستوردة لا على ضوضاء الباني). حقل 16×16 يقود شبكة 97×97 دون أن تملي دقة الشبكة بيانات الارتفاع.
  - **الحتمية بالبناء**: hash سابق شبكي صحيح لا يمر عبر float، والمساران كلاهما نقي — استدعاءان متطابقان بِت-بِت.
- [x] **الوصولية (Rule 0)**: `--heightmap <path>` في `NFSampleBasic3D` هو المستدعي الحقيقي الأول خارج الاختبارات. ملف غير مقروء يقع على الضوضاء مع تسجيل السبب (`ERROR ... unusable (Decode failed ...) — using procedural noise`) لا انقطاع صامت. طُبّق على راحة PGM 16×16: الحقل 16×16، الشبكة 9409 رأس / 55296 فهرس، وارتفاع التضاريس عند الأصل **1.000** بالضبط.
- [x] **النتيجة**: `RHITests` من 170 إلى **182** (12 اختبار ارتفاع جديدة + اختبارات التضاريس القائمة كاملة خضراء). الثغرة المتبقية مُؤجّلة: `uv1` يُكتب ويُمرَّر عبر `MeshUpload`/`LodGenerator` لكنه **ليس** في تخطيط رؤوس `Renderer3D` (position/normal/uv0 فقط) ولا يقرأ `gbuffer.vert` سوى `in_uv0` — فالتغطية تُحمل ولا تُظلَّل بعد. هذا في ملفات مطور آخر النشطة، فيُنتظر استقرارها.

### المرحلة 18: طبقة ثنائية الأبعاد المستقلة (`NFScene2D`)
- [x] وحدة CMake مستقلة عن Jolt/RHI: `Engine/Scene2D` (`DEPENDS NFCore`) مربوطة من الجذر.
- [x] أطلس رفّي حتمي (فرز الارتفاع ثم العرض ثم الاسم) + دفعة رباعيات بـ `stable_sort` على `(page, depth)`.
- [x] خريطة بلاط مقطّعة 16×16 بإحداثيات موقّعة، دمج تصادم جشع، A* ثماني مع كسر التعادل، وبث حلقات.
- [x] محلّل 2D: دائرة/صندوق، تجزئة مكانية، SAT، نبضات متتابعة، قيود مسافة/مفصل، مقابض جيل، `is_deterministic()`.
- [x] إضاءة 2D على الـ CPU بلا أي تبعية RHI: أضواء شعاعية/مخروطية، ثلاثة أنواع تلاشٍ، رمشة (`flicker`) هاش-مبذرة، وظلال مخبوزة على شكل إسفين (ظل الحافة المستقيمة = زاوية بين شعاعين تُقطع عند نصف القطر). الخبز والاختبار الشعاعي يتطابقان بِت-بِت أينما يصل الضوء، والإضاءة تُطوى في لون RGBA8 المكدوس بدل تمريرها للـ GPU.
- [x] أنظمة ECS: تحويل، تصيير مُضاء وغير مُضاء، إضاءة (تجمع الأضواء والحواجز من خريطة البلاط وتخبز)، خريطة بلاط، فيزياء، جسيمات — كلها على `nf::ecs::World` جنبًا إلى جنب مع لعبة 3D.
- [x] جناح `Scene2DTests`: 37/37 (أطلس، دفعة، بلاط، تنقل، محلّل 2D حتمي، مقابض، قيود، شعاع، محاذاة بكسل، إضاءة وظلال).

### إضافات خارج الخارطة (مكتملة)
- [x] دورة الليل والنهار (Time-of-Day: شمس وسماء ديناميكية).
- [x] توليد الأراضي الإجرائية (Heightfield + value-noise حتمي).
- [x] تسجيل وإعادة تشغيل المدخلات (Replay حتمي).
- [x] معالج الأعطال (Minidump + SEH).
- [x] مؤثرات ما بعد المعالجة (Vignette + Saturation) واهتزاز الكاميرا.
- [x] نافذة Profiler حية في المحرر + تصدير Chrome Trace.
- [x] نظام Quest/Inventory/Dialogue وGameplay Tags.
- [x] شخصيات متحركة فيزيائياً (Character Controller) وIK تحليلي.
- [x] ذكاء اصطناعي: A* شبكي + Behavior Trees.
- [x] **نواة الذكاء الاصطناعي الكاملة (§45)**: إدراك (Perception) + آلات حالة (State Machines) + ذكاء فائدة (Utility AI) جنبًا إلى جنب مع Behavior Trees والـ Blackboard — كلها حتمية، وكلها على الـ CPU، وكلها تُقرأ وتكتب Blackboard لا العالم مباشرة (انظر قسم المرحلة 22).
- [x] جزيئات حتمية (Particles) وشبكات موثوقة (Reliable + Snapshots).
- [x] ربط ملفات الصوت بالمشاهد (`buffer=` يُفك تلقائياً).

---

## آلية المشاركة والمساهمة

1. اختيار إحدى النقاط المفتوحة في الجدول أعلاه أو في قسم الـ [Issues](https://github.com/abdallah2183/SANAD-Engine/issues).
2. كتابة تعليق في الـ Issue لتأكيد رغبتك بالعمل عليها لتجنب تكرار الجهود.
3. البدء في التنفيذ ومشاركة التقدم مع المؤسس عبدالله ومجتمع المطورين للمراجعة والدمج.
