// OggVorbisImpl.cpp — the single translation unit that compiles the
// vendored stb_vorbis implementation (OGG Vorbis decoder).
//
// AudioDecoder.cpp includes the same file with STB_VORBIS_HEADER_ONLY for
// the declarations; this TU provides the definitions.

#include "stb_vorbis.c"
